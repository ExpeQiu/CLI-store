# 飞书 Bitable 同步（可选）
FEISHU_APP_SECRET=

# 与 --demo 等价，verify.sh 离线验收用
INVEST_MOCK_MODE=false

# 状态文件目录（默认 ~/.invest，存在 ~/.hermes/invest 时自动回退）
# INVEST_STATE_DIR=~/.invest
# INVEST_STATE_FILE=~/.invest/invest_state.json
