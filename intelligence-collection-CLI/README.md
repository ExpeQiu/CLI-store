# intel-collect

行业情报采集 CLI。

## 安装

```bash
pip install -e .
intel-collect --version
```

## 快速开始

```bash
intel-collect intel feed --demo --format json
intel-collect intel feed --topic 电池 --demo
```

## 验证

```bash
./verify.sh
```

## Agent 接入

见 [SKILL.md](SKILL.md)。
