---
name: intel-collect
description: 行业情报采集 CLI
version: 0.1.0
metadata:
  cli_command: intel-collect
  requires_install: true
  agent_safe_commands:
    - intel-feed-demo
---

# intel-collect

```bash
intel-collect intel feed --demo --format json
```
